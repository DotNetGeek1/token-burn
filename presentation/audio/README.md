# Production SFX (#42)

Drop authored files here using the cue names in `ui/common/ui_sound.gd`:

`tap`, `accept`, `buy`, `burn`, `proc`, `proc_mid`, `proc_high`, `complete`, `cascade`, `error`, `alarm`, `key`, `combo`, `fanfare`

Formats: `.wav` or `.ogg`. Missing files fall back to the synthesised tones. Mute/volume still come from the player profile.
